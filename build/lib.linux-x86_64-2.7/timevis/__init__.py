from timevis.app import app
import views
import api
import models
from plugin import Plugin

# import imp
#
# filenames = ['test']
# pathnames = 'path'
# for file in filenames:
#     f, p, d = imp.find_module(file, [pathnames])
#     imp.load_module(file, f, p, d)
